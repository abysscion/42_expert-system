package Core;

import AST.ASTNode;
import AST.ASTNodeFabric;

import java.util.HashSet;
import java.util.Map;

public class GlobalGraph {
    boolean[] execute(ASTNode tree, String factName, boolean[] fact){
        BooleanFunctionFabric fabric = new BooleanFunctionFabric();
       // if (tree == null || tree.)
       // boolean[] res  = fabric.selectOperation("", new boolean[]{}, new boolean[]{});
        //astNode

        return new boolean[]{};
    }

   /* boolean[] perform(boolean[] left, boolean[] right){

    }*/
}
